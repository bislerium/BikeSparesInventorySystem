@if (user.Item2 is null)
{
    <MudIcon Icon="@Icons.Material.Filled.QuestionMark" Title="Null" />    
} else
{
    <MudChip Variant="Variant.Text" Icon="@Icons.Material.Filled.AccountCircle" Color="@(user.Item1 ? Color.Warning : Color.Default)" Text="@user.Item2" />
}


@code {
    [Parameter]
    public Tuple<bool, string> user { get; set; }
}