<MudChip Variant="Variant.Outlined" Icon="@Icons.Material.Filled.AccountCircle" Color="@(user.Item1 ? Color.Warning : Color.Default)">
    @user.Item2
</MudChip>

@code {
    [Parameter]
    public Tuple<bool, string> user { get; set; }
}